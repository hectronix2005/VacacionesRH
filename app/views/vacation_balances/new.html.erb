<div class="min-h-screen bg-gray-50 py-6">
  <div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Header -->
    <div class="mb-8">
      <div class="flex items-center justify-between">
        <div>
          <h1 class="text-3xl font-bold text-gray-900">
            Nuevo Balance de Vacaciones
          </h1>
          <p class="mt-1 text-sm text-gray-600">
            Crear un nuevo balance de vacaciones para un empleado
          </p>
        </div>
        <div>
          <%= link_to "Volver", vacation_balances_path,
                      class: "inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
        </div>
      </div>
    </div>

    <!-- Form -->
    <div class="bg-white shadow rounded-lg">
      <div class="px-4 py-5 sm:p-6">
        <%= render 'form',
                   vacation_balance: @vacation_balance,
                   users: @users,
                   cancel_path: vacation_balances_path,
                   submit_text: "Crear Balance" %>
      </div>
    </div>

    <!-- Help Text -->
    <div class="mt-6 bg-blue-50 rounded-lg p-4">
      <div class="flex">
        <div class="flex-shrink-0">
          <svg class="h-5 w-5 text-blue-400" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd" />
          </svg>
        </div>
        <div class="ml-3 flex-1 md:flex md:justify-between">
          <p class="text-sm text-blue-700">
            <strong>Información importante:</strong><br>
            • Si no especificas los días totales, se calcularán automáticamente según el país del empleado.<br>
            • Los días usados pueden ser modificados más tarde cuando se aprueben solicitudes.<br>
            • Un empleado solo puede tener un balance por año.
          </p>
        </div>
      </div>
    </div>
  </div>
</div>
